import React from 'react'

export const Create = () => {
  return (
    <div>
        {/* The page to serve tabbed menu for creating both new posts and properties
            (CreatList & CreatePost), Nested routes to serve both pages as outlets in here.
        */}
    </div>
  )
}
